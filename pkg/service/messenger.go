package service

func (s *Service) SendMessage(senderName *string, recipientName *string) error {
	/*
		senderID, err := s.repos.GetUserIDByUsername(senderName)
		if err != nil {
			return err
		}

		recipientID, err := s.repos.GetUserIDByUsername(senderName)
		if err != nil {
			return err
		}
	*/

	return nil
}
